<Window x:Class="SourceSoundScripter.DialogueEditor"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SourceSoundScripter"
        mc:Ignorable="d"
        Title="Dialogue Editor" Height="550" Width="800" MinWidth="600" MinHeight="450">
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="10"/>
			<ColumnDefinition Width="120"/>
			<ColumnDefinition Width="120"/>
			<ColumnDefinition Width="*"/>
			<ColumnDefinition Width="120"/>
			<ColumnDefinition Width="120"/>
			<ColumnDefinition Width="10"/>
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="10"/>
			<!--Dialogue List-->
			<RowDefinition Height="*"/>
			<RowDefinition Height="10"/>
			<!--Dialogue Options-->
			<RowDefinition Height="auto"/>
			<RowDefinition Height="auto"/>
			<RowDefinition Height="auto"/>
			<RowDefinition Height="auto"/>
			<RowDefinition Height="auto"/>
			<!--<RowDefinition Height="*"/>-->
			<RowDefinition Height="auto"/>
			<!--Footer-->
			<RowDefinition Height="auto"/>
			<RowDefinition Height="10"/>
		</Grid.RowDefinitions>

		<!--Dialogue List-->
		<DataGrid Name="DialogueList" ItemsSource="{Binding}"
				AutoGenerateColumns="False" CanUserAddRows="False" SelectionUnit="Cell"
				Grid.Column="1" Grid.Row="1" Grid.ColumnSpan="5" Grid.RowSpan="1">
			<DataGrid.Columns>
				<DataGridTextColumn Header="Name" Width="200" Binding="{Binding Name}" IsReadOnly="True"/>
				<DataGridCheckBoxColumn x:Name="DialogueList_HasPhonemes" Visibility="Collapsed" Header="Phonemes" Width="80" Binding="{Binding HasPhonemes}" IsReadOnly="True"/>
				<DataGridTextColumn Header="Line" Width="500" Binding="{Binding Caption}"/>
			</DataGrid.Columns>
			<DataGrid.CommandBindings>
				<CommandBinding Command="Paste" Executed="DialogueList_Paste" />
				<CommandBinding Command="Delete" Executed="DialogueList_Delete" />
			</DataGrid.CommandBindings>
		</DataGrid>

		<!--Phoneme Options-->
		<Button x:Name="PhonemeScan" Grid.Row="3" Grid.Column="1"
				HorizontalAlignment="Stretch"
				FontSize="14" Content="Scan Phonemes"
				Margin="5,5,5,5" Click="PhonemeScan_Click"
				ToolTip="Scans the sounds for phonemes and marks them."/>

		<!--Caption Options-->
		<Button x:Name="CaptionLoad" Grid.Row="3" Grid.Column="4"
				HorizontalAlignment="Stretch"
				FontSize="14" Content="Load Captions"
				Margin="5,5,5,5" Click="CaptionLoad_Click"
				ToolTip="Loads captions for these sounds from a captions .txt."/>
		<Button x:Name="CaptionCopy" Grid.Row="3" Grid.Column="5"
				HorizontalAlignment="Stretch"
				FontSize="14" Content="Copy Captions"
				Margin="5,5,5,5" Click="CaptionCopy_Click"
				ToolTip="Loads captions for these sounds from a captions .txt."/>
		<TextBlock x:Name="CaptionPrefix_Txt" Grid.Column="4" Grid.Row="4" Margin="2,2,2,2"
				Text="Caption Prefix:" FontSize="12" VerticalAlignment="Center"/>
		<TextBox x:Name="CaptionPrefix" Grid.Column="4" Grid.Row="5"
 				Grid.ColumnSpan="2"
 				FontSize="12"
 				Margin="2,2,2,2"/>
	</Grid>
</Window>
